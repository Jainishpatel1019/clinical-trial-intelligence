# Optional: offline benchmarks, drift UI, Locust, RAGAS. Not installed in production Docker image by default.
# pip install -r requirements.txt -r requirements-evaluation.txt

evidently>=0.4.40
locust>=2.24.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
ragas>=0.1.4
datasets>=2.16.0
requests>=2.31.0
